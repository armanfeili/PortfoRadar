# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=info                # debug | info | warn | error

# Database
MONGO_URI=mongodb://localhost:27017/portfolioradar

# Rate limiting (requests per window)
THROTTLE_TTL=60
THROTTLE_LIMIT=100

# CORS - Controls which frontend domains can call this API
# Only needed if you have a SEPARATE frontend app calling this backend
# Example: If you have a React app at https://my-app.com calling this API
# ALLOWED_ORIGINS=https://my-app.com,https://admin.my-app.com
#
# For your production API (https://portforadar-production.up.railway.app):
# - Leave unset if the API serves its own Swagger UI (current setup)
# - Set if you add a separate frontend that calls this API
# ALLOWED_ORIGINS=https://your-frontend-domain.com

# Scheduled Ingestion (automatic data refresh)
ENABLE_SCHEDULED_INGEST=true          # Enabled by default - auto-fetches KKR data
INGEST_CRON=0 3 * * *                 # Cron expression (default: daily at 3 AM UTC)
# Common cron patterns:
#   0 3 * * *     = Daily at 3:00 AM
#   0 */6 * * *   = Every 6 hours
#   0 0 * * 0     = Weekly on Sunday at midnight
#   0 0 1 * *     = Monthly on the 1st at midnight
